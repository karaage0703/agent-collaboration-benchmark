# Benchmark task: build Collab Tasks

Implement the complete application described in `specs/sample-app.md`.

Constraints:

- Create only the five files listed by the specification.
- Do not change `AGENTS.md`, `README.md`, `package.json`, `specs/`, `benchmark/`, or `skills/`.
- Use no dependencies and make no network requests.
- Make the first implementation edit within three exploration tool rounds.
- Run the repository tests and checks before reporting completion.
- Do not commit or push.

Report changed files, exact test results, and unresolved limitations.
